category: added
summary: "Primitives now implement __hash__"
description: |
  So far, the primitives including `Undefined`, `Error`, `HTCInt`, `HTCFloat`,
  `HTCBool` and `HTCStr` did not implement `__hash__`. This was added.
pull requests:
- 16
version: 0.3.0
